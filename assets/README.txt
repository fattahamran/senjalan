Senjalan uses a CSS-rendered wordmark and local gradient artwork so the static
site works without external image requests. Production photography can be added
to this folder later and referenced with relative paths.
